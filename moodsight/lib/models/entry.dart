class Entry {
  final String? selectedMood;
  final String? notes;
  final String? gratitude;

  Entry({
    this.selectedMood, //Como te sentiste en el día (raton emoji)
    this.notes, //Notas del día
    this.gratitude, //Cosa de la que estés agradecido en el día
  });
}
